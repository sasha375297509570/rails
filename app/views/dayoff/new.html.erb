<h4>Добавить выходной или отгул</h4>

<%= simple_form_for @dayoff, url: create_dayoff_path do |f| %>
	<%= f.error_notification %>

	<%= f.association :employee %>
		
	<%= date_field(:employees_day, :day_start) %>
	<%= f.button :submit %>
<% end %>

